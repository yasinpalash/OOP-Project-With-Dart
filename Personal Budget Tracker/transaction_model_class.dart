class Transaction {
  int id;
  String title;
  double amount;
  DateTime date;
  String type;
  String category;
  Transaction(
      this.id, this.title, this.amount, this.date, this.type, this.category);
}
